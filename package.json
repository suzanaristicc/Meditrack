{
  "name": "meditrack",
  "version": "1.0.0",
  "private": true,
  "description": "MediTrack - fullstack demo for medication ordering and stock management",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "test": "npm run test -w apps/api",
    "seed": "npm run seed -w apps/api",
    "format": "prettier --write .",
    "reset": "npm run reset -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=22.16.0"
  }
}
